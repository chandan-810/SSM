import React from "react";
import { memo } from "react";

const Dashboard = () => {
  return (
    <div>
      <div>Hello World, this is Parent Dashboard</div>
    </div>
  );
};

export default memo(Dashboard);
